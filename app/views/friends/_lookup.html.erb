
<th colspan="3" class="page-header">
  <h2>My friends</h2>
</th>

<div id="friend-lookup">
  <!-- the below "remote: true" statement is essentially
        evoking ajax. After adding this, need to add JS
        partial (_result.js.erb) to display the result.
        Also need to update (add respond_to) search method
        in stocks controller.
  -->
  <%= form_tag search_friends_path, remote: true, method: :get, id: "friend-lookup-form" do %>
    <div class="form-group row no-padding text-center col-md-12">
      <div class="col-md-10">
        <%= text_field_tag :search_param, params[:search_param], placeholder: "Enter name or email",
                          autofocus: "true", class: "form-control search-box input-lg" %>
      </div>
      <div class="col-md-2">
        <%= button_tag(type: :submit, class: "btn btn-lg btn-info") do %>
        <!-- <i class="material-icons">search</i>Look up a stock -->
        <span class="glyphicon glyphicon-search"></span> Search
        <% end %>
      </div>
    </div>
  <% end %>
</div>
